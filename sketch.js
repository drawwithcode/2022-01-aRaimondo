let angle = 0;      //angolo del primo cerchio
let direzione = "destra";
let x = 200;

function setup() {
  createCanvas(windowWidth, windowHeight);
  rectMode(CENTER)
  angleMode(DEGREES)
  background("#AE412A");
  frameRate(40);

}